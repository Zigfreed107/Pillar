# Archive

This folder holds superseded or historical implementation notes that are useful for reference but should not be treated as the active source of truth.

The original moved documents remain in `DocumentationBackUp`, which is intentionally left untouched.

Files placed here should be short summaries or cleaned-up archive copies of earlier planning material, not the primary documentation for current architecture decisions.
