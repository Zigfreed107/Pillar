<!--
    Pillar.CheckBox.xaml
    Defines the reusable checkbox appearance for Pillar's compact dark CAD interface.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Pillar checkboxes own their indicator and interaction states instead of using system theme colors. -->
    <Style x:Key="PillarCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Element1}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource Pillar.FontFamily.UI}" />
        <Setter Property="FontSize" Value="{DynamicResource Pillar.FontSize.ControlContent}" />
        <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.TextPrimary}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent"
                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="IndicatorBorder"
                                Width="16"
                                Height="16"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <Path x:Name="CheckMark"
                                  Margin="2"
                                  Data="M 1.5,6 L 4.5,9 L 10.5,2.5"
                                  Stretch="Uniform"
                                  Stroke="{DynamicResource Pillar.Brush.Background1}"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  StrokeStartLineCap="Round"
                                  StrokeThickness="2"
                                  Visibility="Collapsed" />
                        </Border>

                        <ContentPresenter Grid.Column="1"
                                          Margin="7,0,0,0"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="IndicatorBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Pillar.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="IndicatorBorder"
                                    Property="Background"
                                    Value="{DynamicResource Pillar.Brush.Element2}" />
                            <Setter TargetName="IndicatorBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Pillar.Brush.AccentHover}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="IndicatorBorder"
                                    Property="Background"
                                    Value="{DynamicResource Pillar.Brush.Accent}" />
                            <Setter TargetName="IndicatorBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Pillar.Brush.AccentHover}" />
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="IndicatorBorder"
                                    Property="Background"
                                    Value="{DynamicResource Pillar.Brush.AccentHover}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.TextMuted}" />
                            <Setter TargetName="IndicatorBorder"
                                    Property="Background"
                                    Value="{DynamicResource Pillar.Brush.Background2}" />
                            <Setter TargetName="IndicatorBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Pillar.Brush.Border}" />
                            <Setter TargetName="CheckMark"
                                    Property="Stroke"
                                    Value="{DynamicResource Pillar.Brush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
