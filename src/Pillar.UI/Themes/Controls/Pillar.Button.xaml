<!--
    Pillar.Button.xaml
    Defines standard and workflow-specific Button styles for the Pillar workspace.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Standard buttons own their chrome and states without relying on the WPF theme. -->
    <Style x:Key="PillarButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="11,5,11,6" />
        <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource Pillar.FontFamily.UI}" />
        <Setter Property="FontSize" Value="{DynamicResource Pillar.FontSize.ControlContent}" />
        <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.Foreground}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- Keep all button states under Pillar's control rather than Fluent's internal template. -->
                <ControlTemplate TargetType="Button">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Pillar.CornerRadius.Control}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.FocusBorder}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.HoverBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.HoverBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.HoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.PressedBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.PressedBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.PressedForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.DisabledBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.DisabledBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.DisabledForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PillarButtonAccentStyle" TargetType="Button">
        <Setter Property="Padding" Value="11,5,11,6" />
        <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.HoverBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.HoverBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource Pillar.FontFamily.UI}" />
        <Setter Property="FontSize" Value="{DynamicResource Pillar.FontSize.ControlContent}" />
        <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.Foreground}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- Keep all button states under Pillar's control rather than Fluent's internal template. -->
                <ControlTemplate TargetType="Button">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Pillar.CornerRadius.Control}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.FocusBorder}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.HoverBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.HoverBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.HoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.PressedBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.PressedBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.PressedForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.DisabledBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.DisabledBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.DisabledForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PillarButtonSubtleStyle" TargetType="Button">
        <Setter Property="Padding" Value="11,5,11,6" />
        <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Element1}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource Pillar.FontFamily.UI}" />
        <Setter Property="FontSize" Value="{DynamicResource Pillar.FontSize.ControlContent}" />
        <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.Foreground}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- Keep all button states under Pillar's control rather than Fluent's internal template. -->
                <ControlTemplate TargetType="Button">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Pillar.CornerRadius.Control}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.FocusBorder}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.HoverBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.HoverBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.HoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.PressedBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.PressedBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.PressedForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Button.DisabledBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Button.DisabledBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.Button.DisabledForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Main Mode panel buttons launch individual tools from the workflow tabs. -->
    <Style x:Key="PillarModePanelToolButtonStyle"
           BasedOn="{StaticResource PillarButtonStyle}"
           TargetType="Button">
        <Setter Property="Width" Value="96" />
        <Setter Property="Height" Value="106" />
        <Setter Property="Margin" Value="6,6,6,6" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Element1}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Element2}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Accent}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.TextPrimary}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.AccentSubtleTransparent}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Accent}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.TextPrimary}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Pillar.Brush.Element1}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Pillar.Brush.Border}" />
                <Setter Property="Foreground" Value="{DynamicResource Pillar.Brush.TextPrimary}" />
                <Setter Property="Opacity" Value="0.48" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
