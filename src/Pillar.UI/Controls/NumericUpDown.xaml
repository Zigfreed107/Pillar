<!--
    NumericUpDown.xaml
    Provides a reusable WPF numeric editor for tool settings without introducing third-party UI dependencies.
-->
<UserControl x:Class="Pillar.UI.Controls.NumericUpDown"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Pillar.UI.Controls"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             Focusable="False"
             PreviewMouseWheel="NumericUpDown_PreviewMouseWheel">
    <UserControl.Resources>
        <!-- Numeric entry does not use the Fluent TextBox clear button because an empty value is invalid here. -->
        <Style x:Key="NumericValueTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SpinnerButtonStyle"
               TargetType="{x:Type primitives:RepeatButton}">
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background"
                    Value="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type controls:NumericUpDown}}}" />
            <Setter Property="BorderBrush"
                    Value="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type controls:NumericUpDown}}}" />
            <Setter Property="Foreground"
                    Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type controls:NumericUpDown}}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:RepeatButton}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource Pillar.Brush.Element2}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource Pillar.Brush.Accent}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource Pillar.Brush.AccentSubtleTransparent}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource Pillar.Brush.Accent}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{Binding Background, ElementName=Root}"
        BorderBrush="{Binding BorderBrush, ElementName=Root}"
        BorderThickness="{Binding BorderThickness, ElementName=Root}"
        CornerRadius="{StaticResource Pillar.CornerRadius.Control}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="24" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ValueTextBox"
                     Grid.Column="0"
                     Style="{StaticResource NumericValueTextBoxStyle}"
                     MinHeight="24"
                     Padding="4,1"
                     Background="Transparent"
                     BorderBrush="{Binding BorderBrush, ElementName=Root}"
                     BorderThickness="0"
                     FontFamily="{Binding FontFamily, ElementName=Root}"
                     FontSize="{Binding FontSize, ElementName=Root}"
                     FontWeight="{Binding FontWeight, ElementName=Root}"
                     Foreground="{Binding Foreground, ElementName=Root}"
                     VerticalContentAlignment="Center"
                     GotKeyboardFocus="ValueTextBox_GotKeyboardFocus"
                     LostKeyboardFocus="ValueTextBox_LostKeyboardFocus"
                     PreviewKeyDown="ValueTextBox_PreviewKeyDown"
                     PreviewTextInput="ValueTextBox_PreviewTextInput"
                     DataObject.Pasting="ValueTextBox_Pasting" />

            <TextBlock x:Name="UnitTextBlock"
                       Grid.Column="1"
                       Margin="6,0,4,0"
                       VerticalAlignment="Center"
                       FontFamily="{Binding FontFamily, ElementName=Root}"
                       FontSize="{Binding FontSize, ElementName=Root}"
                       Foreground="{Binding Foreground, ElementName=Root}"
                       IsHitTestVisible="False"
                       Text="{Binding UnitText, ElementName=Root}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Grid Grid.Column="2"
                  Margin="2,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <primitives:RepeatButton x:Name="IncreaseButton"
                                         Grid.Row="0"
                                         Style="{StaticResource SpinnerButtonStyle}"
                                         HorizontalAlignment="Stretch"
                                         VerticalAlignment="Stretch"
                                         Delay="300"
                                         Interval="70"
                                         Click="IncreaseButton_Click"
                                         ToolTip="Increase value"
                                         AutomationProperties.Name="Increase value"
                                         BorderThickness="0">
                    <Path Width="7"
                          Height="4"
                          Stretch="Fill"
                          Data="M 0,4 L 3.5,0 L 7,4 Z"
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type primitives:RepeatButton}}}" />
                </primitives:RepeatButton>
                <primitives:RepeatButton x:Name="DecreaseButton"
                                         Grid.Row="1"
                                         Style="{StaticResource SpinnerButtonStyle}"
                                         HorizontalAlignment="Stretch"
                                         VerticalAlignment="Stretch"
                                         Delay="300"
                                         Interval="70"
                                         Click="DecreaseButton_Click"
                                         ToolTip="Decrease value"
                                         AutomationProperties.Name="Decrease value"
                                         BorderThickness="0">
                    <Path Width="7"
                          Height="4"
                          Stretch="Fill"
                          Data="M 0,0 L 3.5,4 L 7,0 Z"
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type primitives:RepeatButton}}}" />
                </primitives:RepeatButton>
            </Grid>
        </Grid>
    </Border>

</UserControl>
